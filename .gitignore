*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor

# Build output stays out of git. Hostinger cannot build (proc_open is disabled
# so Composer will not run, and Node is not installed), so GitHub Actions builds
# vendor/ and public/build on its own runner and ships them as a tarball.
# See .github/workflows/deploy.yml and deploy.sh.
/build.tar.gz
Homestead.json
Homestead.yaml
Thumbs.db
